apiVersion: v1
kind:  Service
metadata: 
  name: mgt-frontend-test-service
  namespace: test-env
  labels:
    app: mgt-frontend
    env: test
spec:
  selector:
    app: mgt-frontend
    env: test
  ports:
    - port: 80
      targetPort: 8301
  type: NodePort
